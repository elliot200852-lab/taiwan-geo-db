# Python build venv（依 requirements.txt 重建，不進 repo）
.venv/
__pycache__/
*.pyc
.DS_Store

# 圖片 SSOT 在 Google Drive、由 CI 的 scripts/pull_images.py 拉取（見 docs/DEPLOY.md）。
# 只擋子目錄：site/img/manifest.json 是 build.py 的輸入、必須留在 repo。
site/img/*/
_workspace/

# fact_gate.py --fetch 的事實佐證快取（來源網站全文，2026-09-13 從 MyWork/_workspace/
# geo-tn-b2/.source-cache 搬回來）。來源網站下架就無法重建，本機留著但不進 git。
.source-cache/
